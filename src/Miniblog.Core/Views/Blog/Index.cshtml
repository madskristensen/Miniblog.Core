@model IEnumerable<Post>
@inject IOptionsSnapshot<BlogSettings> settings
@{
    ViewData["Title"] = settings.Value.Name;
}

@{
    foreach (var post in Model)
    {
        await Html.RenderPartialAsync("post", post);
    }
}